import{colors} from '../../constants/colors.js'

export const VerificationColors = {
        title: colors.mainColor,
        borderInputColor: colors.mainColor,
        Btn: {
          titleColor: 'white',
          backgroundColor: colors.mainColor,
        },
    }