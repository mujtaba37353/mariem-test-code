import { colors } from "../../../../constants/colors";

export const CardColors = {
  colors: {
    titleColor: colors.mainColor,
    descColor: colors.mainColor,
    borderColor: 'transparent',
    hoverColor: 'transparent',
    cardContentBg: colors.whiteColor,
  },
}
